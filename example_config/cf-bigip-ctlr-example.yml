bigip:
  url:
  user:
  pass:
  partition:
  balance:

nats:
  - host:
    port:
    user:
    pass:

logging:
  syslog:
  level:
  loggregator_enabled:
  metron_address:

go_max_procs:
prune_stale_droplets_interval:
droplet_stale_threshold:
suspend_pruning_if_nats_unavailable:

oauth:
  token_endpoint:
  client_name:
  client_secret:
  port:
  skip_ssl_validation:
  ca_certs:

routing_api:
  uri:
  port:
  auth_disabled:

start_response_delay_interval:

token_fetcher_max_retries:
token_fetcher_retry_interval:
token_fetcher_expiration_buffer_time:
